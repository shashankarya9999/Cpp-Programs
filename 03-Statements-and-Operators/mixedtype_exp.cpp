#include<iostream>

using namespace std;

int main()
{
    /*
    
        // the lower is promoted to a higher
        lower (operator) higher 
        2 * 5.2     // 2 is promoted to 2.0

        // the higher is demoted to lower
        lower = higher
        int num {0};
        num = 100.2; 

    */

    return 0;
}