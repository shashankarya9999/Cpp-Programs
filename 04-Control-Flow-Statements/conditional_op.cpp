#include<iostream>

using namespace std;

int main()
{
    /*
    
    // Conditional Operator Examples

    int a {10}, b {20};
    int score {92};
    int result {};

    result = (a > b) ? a : b;

    result = (a < b) ? (b - a) : (a - b);

    result = (b != 0) ? (a / b) : 0;

    cout << ((score > 90) ? "Excellent" : "Good");
    
    */

    return 0;
}