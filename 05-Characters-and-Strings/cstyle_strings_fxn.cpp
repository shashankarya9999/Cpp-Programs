#include<iostream>
#include<cstring>

int main()
{
    /*
        char str[80];
    
        strcpy(str, "Hello ");      // copy
    
        strcat(str, "there");       // concatenate
    
        cout << strlen(str);        // 11
    
        strcmp(str, "Another");     // > 0
    */

    return 0;
}
